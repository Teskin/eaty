import React from 'react';
import {Link} from 'react-router-dom';

const StartButton = () => {
  return <Link className='btn btn-primary' to='/types-of-food'>Start</Link>;
}

export default StartButton;
